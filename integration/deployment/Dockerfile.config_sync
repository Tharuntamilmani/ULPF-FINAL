FROM python:3.12-slim

WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn httpx pydantic structlog pyyaml

COPY integration /app/integration

ENV PYTHONPATH=/app
EXPOSE 8081

CMD ["uvicorn", "integration.config_sync.config_worker:config_sync_app", "--host", "0.0.0.0", "--port", "8081"]
